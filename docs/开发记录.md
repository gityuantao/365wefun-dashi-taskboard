# 开发记录

## 2026-08-10：ClickUp 评论图片作为 AI 输入

分析、开发和验收作业现在会读取 ClickUp 最近评论中的图片附件，并以 Codex 的真实图片输入传入。只有图片、没有文字的评论同样会被保留；文字使用「评论 `<commentId>`：…」，图片使用「评论 `<commentId>` 图片：`<filename>`」，因此两类内容都能回溯到原评论。

收集器兼容 ClickUp 当前返回的富文本结构：图片位于 `comment[].type=image` 段的 `image` 对象中，同时保留旧的顶层 `image` / `images` / `attachments` 形态。相同下载 URL 只处理一次；PDF 等明确的非图片通用附件会被过滤，不会阻断图片评论。

### 支持范围与默认限制

- 只接受 PNG、JPEG、WebP 和 GIF；除文件头和响应 MIME 比对外，还校验 PNG chunk/IEND、JPEG segment/EOI、WebP RIFF chunk 长度和 GIF block/trailer。HTML 错误页、签名截断、容器不完整或 MIME 不匹配都会在进入 Codex 前拒绝。
- 每次作业选择最近 12 条评论，最多读取 8 张图片，单图上限 10,000,000 字节，总量上限 30,000,000 字节。
- 下载使用流式读取；收到响应头后先检查 `Content-Length`，逐块读取时再次执行硬上限，越界立即取消流。整个 fetch 与读取共用真实的 `AbortController` 超时，超时会中止底层请求。
- 收集器把单图上限与剩余总预算的较小值传给每次下载，并把下载尝试数限制在图片数上限内；超限的旧图片不会继续下载。
- 超出图片数量、单图或总量限制时，优先保留最新反馈，在上下文中标记具体评论、文件名和 `IMAGE_LIMIT`，并由每个 AI 阶段向 ClickUp 合并发布一次脱敏诊断，不会静默丢弃或逐图刷屏。

生产客户端无需运行时配置即可接受 `api.clickup.com`、`attachments.clickup.com`、`attachments-public.clickup.com`，以及恰好一层租户前缀的 `*.p.clickup-attachments.com`。只允许 HTTPS、默认端口、无 URL 凭据；后缀欺骗和多层子域不会命中租户规则。显式附加 host allowlist 仍需通过裸主机名校验。

### 失败关闭与排查

评论列表读取失败，或选中的图片缺少可用 URL、下载/鉴权失败、结构损坏、MIME 与字节不匹配时，当前 AI 作业都会失败关闭，不会忽略评论或图片继续执行。分析、开发和验收的任务聚合均进入 `waiting_info`，ClickUp 只记录固定错误类别与脱敏文件名。

结构校验不替代完整像素解码；如果文件通过容器校验但 Codex 仍报告图片解码、格式不支持或损坏，三个 AI 阶段同样转 `waiting_info`，使用 `IMAGE_DECODE_FAILED` 诊断，并清理本地临时图片。Codex stderr 中的本地路径或 URL 不会写回 ClickUp。

运营人员从 ClickUp 评论或作业错误中查看统一诊断：

```text
comment image unavailable: <filename> (<code>)
```

`<filename>` 直接定位无法读取的附件，`<code>` 区分 `IMAGE_UNAVAILABLE`、`INVALID_IMAGE`、`IMAGE_TYPE_MISMATCH`、`ATTACHMENT_HOST`、`HTTP_401` 等原因。诊断不包含 ClickUp token、带签名的附件 URL 或本地临时路径；重新上传该文件后，按 ClickUp 评论中的状态提示重新启动对应阶段。

评论列表本身不可用时采用固定诊断 `comment history unavailable (COMMENTS_UNAVAILABLE)`，不会转发底层异常文本。数量或字节截断采用「部分评论图片未读取」合并评论，每次作业最多发送一次。

### 临时文件清理

图片只写入操作系统临时目录下的 `taskboard-clickup-images-*` 任务专用目录：目录权限为 `0700`，文件权限为 `0600`，上游文件名不用作本地路径。Codex 返回、抛错、超时或取消时均通过同一 `finally` 路径删除整个目录；收集中途失败也会先清理已下载图片再返回错误。这些图片不写入 Git、数据库或长期日志。

## 2026-08-10：iOS 全 App TestFlight 门禁真实验证

编排器的 iOS 提测流程已完成两款 App 真实端到端验证。测试与归档现在是两个独立概念：每个 App 在注册表里显式声明 `testScheme` 和 `testTarget`，自动测试使用 Debug 测试构建以运行测试钩子；真正上传的 App 仍使用 release-optimized `Staging` 归档，并继续校验测试 API、精确 bundle/version/build 和禁止的 Debug 钩子。

此拆分避免把“真机人工 Sandbox 验收”和依赖 Debug 模拟购买的 UI 测试误跑在 Staging/Release 二进制上。当前配置为：

- `E365AU` 归档；`E365AU` scheme 中只运行 `E365StoreKitTests` 自动测试 target。
- `E365CN` 归档；运行 `E365ChinaComplianceTests` scheme/target。

完整编排回归为 446 项全部通过，其中包含“Web 成功不得跳过 iOS”、“第二个 App 未确认时不得进入待测试”以及“两个 App 都确认后只转移一次”的门禁回归。

## 2026-08-11：显式重做恢复与最新验收反馈上下文

根因是普通开发失败行会阻止相同 `auto-develop-<taskId>` 再次入队；任务在验收不通过后由用户明确改回「待开发」时，状态机虽然进入重做路径，但旧的普通失败行仍在，因此这次显式重做没有产生新的开发作业。

本次修复新增两条会清除该任务旧普通开发失败行的明确用户重做路径：`acceptance_rejected` 改回「待开发」，以及测试/验收后的 `ready_for_test` 改回「待开发」；此前已有的用户从「待开发」手动切到「开发中」路径也会执行相同清理。删除范围严格限定为相同 `auto-develop-<taskId>`、`status = 'failed'`，且结果不属于 `waiting_version`、`waiting:`、`needs_human` 或 `needs_info` 的行；除上述三条明确用户状态变更路径外，其他无关更新或陈旧快照不会解锁普通失败。修复提交为 `333bfe54ebc2a247e60188d9938b112adf8a1c62`。

开发作业继续把验收反馈字段与最新 12 条 ClickUp 评论一起传给 Codex。评论无论 ClickUp 返回为正序还是 newest-first，都会按时间选出最新 12 条并以从新到旧的确定顺序各出现一次；窗口内评论图片保留评论标签并作为真实图片输入，窗口外旧评论及图片不进入提示词。该保证由 `2da475e71248e08b0e2c46780b0340948a202d83` 建立回归覆盖，并由 `f4948c72fa25a5b7ccec69d78af1502580637cba` 加强为完整 12 条窗口、顺序和唯一性断言。

验证结果：focused 回归 `node --test test/orchestration/clickup-poller.test.mjs test/orchestration/developer.test.mjs` 为 76/76 通过；完整 `pnpm test:orchestration` 为 448/448 通过；`node --check cloud/src/clickup-poller.mjs`、`node --check orchestration/ai/developer.mjs` 与 `git diff --check` 均退出 0。

## 2026-08-11：提测失败归属与基础设施恢复

任务进入测试环境部署时，已合并 PR 的 merge commit 不一定仍等于版本分支最新 HEAD：版本分支后续合入其它任务后，旧逻辑把这种正常的历史包含关系误判为 merge 失败；随后 staging 失败又沿用产品返工路径，消耗返工次数并可能错误创建 develop 作业。另一个风险是仅凭历史 staging 失败就恢复，无法证明它属于当前「验收不通过」。

修复后的规则如下：

- 已合并 PR 必须同时证明 PR head 与 merge commit 都是 freshly fetched `origin/<version branch>` 的祖先；版本分支当前 HEAD 作为 Candidate。缺任一 ancestry 证明即失败关闭。
- staging 失败持久化 `failure_owner=staging_infrastructure`、分类和基于完整脱敏错误的 SHA-256 指纹；重复故障不消耗产品返工次数，也不删除历史 staging / D1 证据。
- 仅当当前 rejection provenance 为 `runner-staging`、evidence 指向 staging、最新失败发生在当前 rejection 窗口内，且最新 staging attempt 与历史 `stage_task` payload 的 PR、accepted commit、version branch、target version、platforms 完整一致时，用户显式从「验收不通过」改到「待开发」才产生一次 `retry_staging`。产品验收不通过仍走 develop；未知归属、缺证据、陈旧或不匹配记录全部失败关闭。
- retry 只重放持久化的 stage payload，不创建 develop 作业；staging coordinator 还会校验 accepted commit 与 PR head 相等，并在每个外部边界继续执行 lease/fencing 检查。Web 与全部启用的 iOS/TestFlight gate 均权威 readback 成功后，任务才进入「待测试」。

实现提交：`a8f097f`（已合并 PR ancestry）、`1437464`（staging failure ownership）、`6bd1d21`（完整错误指纹）、`67573e8`（显式 staging 恢复）、`67b3ca8`（当前 rejection provenance 与 fail-closed 加固）。

Fresh 验证：聚焦回归 `node --test test/orchestration/git-merge.test.mjs test/orchestration/staging-coordinator.test.mjs test/orchestration/clickup-poller.test.mjs test/orchestration/mvp-e2e.test.mjs test/orchestration/migration-runner.test.mjs` 为 78/78 通过；完整 `pnpm test:orchestration` 为 464/464 通过；`node --check orchestration/git/merge.mjs`、`node --check orchestration/application/staging-coordinator.mjs`、`node --check cloud/src/clickup-poller.mjs` 与 `git diff --check` 均退出 0。任务 brief 中旧路径 `orchestration/clickup/poller.mjs` 在当前仓库不存在，生产 poller 入口为 `cloud/src/clickup-poller.mjs`。

现场恢复仅重启 supervisor 管理的 orchestrator child：supervisor PID `10351` 未变，child 从 `57240` 更换为 `79032`；47824 实际只读控制端点与 47823 Dashboard 均返回 HTTP 200（当前 47824 不提供 `/health`，该路径返回 404）。只读取证确认 ClickUp 为「验收不通过」，当前 rejection v40 来自 `runner-staging` 且 evidence 精确指向 `stage_task-39`；PR #895 的 head `fff570de…` 与 merge commit `797a13b8…` 均为当时 current `origin/version/v1.0.3` `02ae01f9…` 的祖先。

三条旧 attempt 由旧版本写入，owner 为空。经逐字段交叉当前 rejection、最新 attempt 与原 stage payload 后，只对 `86d3yebnh-staging-3` 做单行 legacy backfill，使用生产相同的完整脱敏规范化输入生成指纹；前两条历史记录、原 error/stage/candidate 与所有 D1 记录均保留。随后只将 ClickUp 从「验收不通过」显式改为「待开发」一次，产生唯一 job `86d3yebnh-stage_task-41`，未产生 develop job。

该 job 在 `persist_candidate` 阶段失败关闭：Candidate 为 `02ae01f9…`，权威错误为 `version branch does not point to frozen Candidate`，attempt 4 持久化 `failure_owner=failure_classification=staging_infrastructure` 和指纹 `1ee03c1f…`。ClickUp 权威回到「验收不通过」；触发后 stage job 数为 1、develop job 数为 0。Web 和两款 iOS/TestFlight gate 尚未开始，未部署生产、未提交 App Review，也未再次触发恢复。

## 2026-08-12：正式版本全自动发布（保持生产 Hold）

正式发布主路径为：Dashboard 精确确认版本 → 冻结 Candidate/Manifest/目标计划 → Web 与 API 分平台原子部署和权威回读 → 注册表内全部 enabled iOS App 依次执行 Release 测试、归档、上传、处理回读、审核资料、提交审核、等待审核和精确线上 build 回读 → 所有目标成功后发布任务与版本 → 持久化幂等清理。发布 attempt/target、外部请求身份、stage、权威证据和失败分类写入 D1；重启从冻结 Manifest 与持久化 target 续跑，成功目标复用，未知 POST 先 GET 对账，租约/fencing 丢失后禁止后续外部动作。

生产运行配置需要 `releaseAdapterModule`、两条 production command、两个私密 descriptor path、ASC HTTPS API、timeouts、lease、reconciliation budget 和完整 enabled iOS App 注册表。秘密只允许放权限受控私密文件或环境，不进入 runtime、D1、日志、ClickUp 或页面。

新增显式 `productionReleaseHold`。只有精确配置为 `false` 才允许 production runtime ready；字段缺失或 `true` 都返回 `held=true`，并在 adapter import 或任何发布动作前失败关闭。示例配置默认 `true`。该开关与全局研发编排 master switch 分离，日常分析、开发和提测可继续运行。

恢复口径：`waiting_external` 只做权威 GET；`external_unknown` 使用持久化 locator/request/idempotency 先回读，只有权威 absence 且预算未耗尽才允许同身份补偿；`release_failed` 不自动重试，必须经精确确认返回 `releasing`；Web/API 切换或健康失败恢复旧入口并持久化 rollback；lease/fencing 丢失立即停止；已发布清理按 cleanup attempt 幂等续跑。

Fresh 无副作用验证：`pnpm test:orchestration` 624/624；production fixture/Manifest、all-App E2E、环境 allowlist、安全与 runtime 子集 58/58；`pnpm build`、三个正式发布脚本 `node --check` 和 `git diff --check` 均退出 0。全仓 `pnpm test` 为 1016 项、997 通过、19 个既有 AI chat/附件/cloud proxy/workflow UI 契约失败；完整名单在客户审计证据中保留，production release suites 独立全绿。

生产 Web、Admin、API readiness 只读请求均 HTTP 200；ASC 对 AU/CN 两个配置 App 的只读 identity GET 均精确匹配 App Store ID 与 bundle ID。私钥存在且权限 `0600`；未输出凭据值。全程未发生 SSH deploy、生产 POST、App 上传、审核或发布。

当前私密 runtime 仍缺经确认的生产部署 descriptor 与 ASC release descriptor，readiness 必须报告配置缺失；不得猜值或用占位值伪报 ready。解除 Hold 与任何真实生产动作必须针对命名验证版本和全部目标另行取得明确授权。

运行态仅重启 supervisor 管理的 child：supervisor PID `10351` 保持，child `16295 → 77604`。重启后 47823/47824 均 HTTP 200；Dashboard readiness 为 `ready=false, held=true, error=productionConfigPath does not exist`。跨过一个 poll interval 后仍为 `releasing=[]`、release activity 计数不变，未自发创建生产发布作业或进入 adapter import/effect。

## 2026-08-12：PR 冲突自动返工、验收 SHA 绑定与 iOS 平台连续性

任务 `86d40ezdx`（PR #915）与 `86d40f2by`（PR #916）开发验收通过后，版本分支已继续前进，原 PR 与 `version/v1.0.3` 发生真实冲突。旧实现只记录 `git merge` 的 stderr，而 Git 冲突证据位于 stdout，导致 D1、ClickUp 和下一轮开发上下文中的 error 为空；同时旧验收结果未绑定当前聚合版本，PR 头在冲突修复后更新仍可能复用旧 accepted SHA。另有分析结果未持久化平台、提测只看开发结果，以及否定句“Android/Web/小程序不在范围”被关键词误判为四平台的问题。

修复后：Git 合并失败收集 stdout、stderr 与 `git diff --name-only --diff-filter=U`，脱敏、有界保存；`merge_conflict` 和 `stale_acceptance` 都归类 `product_rework`，自动返回待开发，并把原 PR、版本分支、冲突文件或新鲜验收要求作为结构化 finding 交给下一轮开发器；accepted 结果必须与当前 aggregateVersion 精确一致。分析结果持久化平台，开发与提测按 ClickUp 显式平台、开发结果、分析结果依次回退；文本推断按分句排除“不涉及/不在范围/无需/不用/不修改”等否定平台。实现提交为 `e5ed5b9`、`0f2d6a4`、`54d050c`、`ef9c875`、`4dcf665`、`4360557`。

Fresh 完整 `pnpm test:orchestration` 最终为 633/633 通过；相关 Node 语法检查与 `git diff --check` 均通过。现场仅重启 supervisor PID 10351 管理的 child，47824 始终恢复 HTTP 200；未手工修改两个任务的业务代码，冲突均由各自 AI 开发器在原任务 Worktree 合并版本分支并更新原 PR。

现场最终证据：`86d40f2by` 原 PR 头更新为 `92e07149…`，提测 payload 精确为 `["ios"]`，AU build 2053 与 CN build 2048 均 `processed` 且 `membership_confirmed=1`，任务于 2026-08-12 01:57:38Z 同步待测试。`86d40ezdx` 在版本分支再次前进后继续自动处理 `apps/ios/project.yml` 冲突，原 PR 头更新为 `da3280d5…`；AU build 2054 与 CN build 2049 均 `processed` 且 `membership_confirmed=1`，任务于 2026-08-12 02:23:03Z 同步待测试。全程未提交生产或 App Review。

## 2026-08-12：v1.0.3 发布就绪证据刷新与状态分叉诊断

版本详情此前报告 34 个任务缺少影响平台，根因不是 ClickUp 字段普遍为空，而是这些任务的旧 D1 快照生成于平台字段纳入快照之前；字段未发生后续状态变化时，增量轮询不会重写该快照。发布就绪工具新增显式 `--refresh-clickup`：只按目标版本中已存任务 ID 逐条 GET ClickUp 权威任务并规范化保存本地快照，不修改 ClickUp、不创建发布 job，也不触发生产适配器。v1.0.3 刷新后，34 个缺失任务中 33 个恢复了 ClickUp 显式平台，只剩 `86d40ejq2` 的权威字段确实为空。

平台来源优先级同时修正：若 staging payload 与最终 accepted commit 精确一致，则已成功提测的平台比开发阶段的宽泛推测更权威。`86d40f2by` 因此从错误的 iOS/Android/Web/小程序收口为真实的仅 iOS，证据为 `stage_task-17` 与 accepted commit `92e07149…`。

页面现在还会单独报告“ClickUp 已是待发布、但编排器 aggregate 尚未就绪”的状态分叉。现存 `86d3wtrwt`、`86d3xfa2r`、`86d3xfb4g`、`86d3xmaw8` 都没有晚于 blocker 的验收通过证据，内部状态分别仍为验收不通过、待开发或待补充信息，所以 blocker 保留且不得为了点亮发布按钮自动删除。

刷新后的真实发布缺口为：`86d40ejq2` 缺影响平台；版本包含 Android 与小程序，但本期正式发布适配器只支持 Web/API/iOS；生产 descriptor 不存在；`productionReleaseHold=true`。这些都继续失败关闭。运行期间没有 SSH 部署、生产 POST、App 上传、审核或发布。

## 2026-08-12：Dashboard 与非当前版本终态同步复核

真实 Chrome 复核确认 v1.0.3 总览与详情统一按内部状态机显示 `27/38` 就绪、11 个状态分叉和 4 个开放 blocker；这些任务均存在真实失败或待补充证据，未强制改为就绪。修复非当前开发版本任务被版本门禁提前跳过的问题：只有内部已为 `ready_for_test` 且 ClickUp 明确变为「待发布」时，轮询器才在门禁前安全补记 `test_passed`；验收不通过、待补充或待开发仍不能越过验收与部署门禁。总览的取消计数改用 ClickUp 权威取消状态，版本详情补齐「验收不通过」中文标签。

开发 AI 的影响平台提示同时恢复为用户可读名称（如「小程序、安卓、iOS」），不再暴露内部 `mini_program/android` 标识。Fresh 验证：聚焦 poller/dashboard/developer 119/119，完整 `pnpm test:orchestration` 658/658，`pnpm test:dashboard` 58/58，typecheck、Web build、Node 语法检查与 `git diff --check` 均通过。生产 Hold 保持启用，未触发真实发布副作用。
